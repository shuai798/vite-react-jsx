import React from 'react';
import { Outlet } from 'react-router-dom';

const BasicLayout = () => {
  return (
    <div>
      111
      <Outlet />
    </div>
  );
};

export default BasicLayout;
